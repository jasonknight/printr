== Welcome to Printr

Printr is an interface to serial/usb/plain_file printers on *nix systems. You can use it with your udev rules etc. It is
built as a rails engine, and should be used as a gem.

You can pull the latest edition from this repo, or:

#Gemfile
gem 'printr'

Then run bundle install

